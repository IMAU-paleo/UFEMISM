&CONFIG

! Start and end time
start_time_of_run_config            = -60000    ! Start time (in years) of the simulations
end_time_of_run_config              =  60000    ! End   time (in years) of the simulations
dt_coupling_config                  = 100

create_new_output_dir_config        = .TRUE.
output_dir_config                   = 'results'

! Output data
save_secondary_data_mesh_config              = .TRUE.
save_secondary_data_ice_dynamics_config      = .TRUE.
save_secondary_data_SMB_components_config    = .TRUE.

! Benchmark experiment
do_benchmark_experiment_config      = .TRUE.
choice_benchmark_experiment_config  = 'EISMINT_1'

nPOI_ANT_config                     = 1
POI_ANT_coordinates_config          = -90.0, 0.0
POI_ANT_resolutions_config          = 32.0

alpha_min_config                    = 0.5
dz_max_ice_config                   = 6000.0      ! Maximum allowed 2nd order surface deviation over ice  (m)
res_max_config                      = 200.0       ! Maximum allowed resolution                            (km)
res_max_margin_config               = 32.0         ! Maximum allowed resolution over land-based ice margin (km)
res_max_gl_config                   = 32.0         ! Maximum allowed resolution over grounding line        (km)
res_max_cf_config                   = 32.0         ! Maximum allowed resolution over calving front         (km)
res_max_mountain_config             = 32.0         ! Maximum allowed resolution over mountains             (km)
mesh_fitness_threshold_config       = 0.9

! Time stepping
dt_max_config                       = 10       ! Maximum time step (in years) of the ice model
dt_thermo_config                    = 5        ! Time step (in years) for updating the thermodynamics
dt_output_config                    = 5000    ! Time step (in years) for writing output
dt_mesh_min_config                  = 50

! Ice dynamics
m_enh_sia_config                    = 1.0
m_enh_ssa_config                    = 1.0

/
